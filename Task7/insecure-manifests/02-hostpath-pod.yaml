apiVersion: v1
kind: Pod
metadata:
  name: pod-hostpath
  namespace: audit-zone
spec:
  containers:
    - name: nginx
      image: nginx
      volumeMounts:
      - name: some-volume
        mountPath: /tmp
  volumes:
    - name: some-volume
      hostPath:
        path: /var/log
        type: Directory